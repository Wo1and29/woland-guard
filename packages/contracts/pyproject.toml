[project]
name = "woland-guard-contracts"
version = "0.1.1"
description = "Versioned data contracts shared by Woland Guard components"
license = "AGPL-3.0-or-later"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pydantic>=2.10,<3",
]

[build-system]
requires = ["uv_build>=0.12.2,<0.13"]
build-backend = "uv_build"
